base_image = "ubuntu:24.04"
instructions = "Create a dedicated working directory with an appropriate name inside the '/workspace' directory and perform all tasks there."

[[mount]]
host = "./workspace"
container = "/workspace"
